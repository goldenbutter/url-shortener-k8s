apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: url-shortener
data:
  USE_REDIS: "true"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  PUBLIC_BASE_URL: "http://localhost:30080"
