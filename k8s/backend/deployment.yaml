apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: url-shortener
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: goldenbutter/url-shortener-backend:v2
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: backend-config